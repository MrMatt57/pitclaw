{
  "name": "pitclaw-simulator",
  "version": "1.0.0",
  "description": "DEPRECATED - Use 'pio run -e simulator' instead. See firmware/src/simulator/.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "npx concurrently --kill-others \"npx nodemon --watch . --ext js,json --ignore node_modules server.js\" \"npx wait-on http://localhost:3000 && npx browser-sync start --proxy localhost:3000 --ws --files '../firmware/data/**/*' --no-open --port 3001\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2",
    "wait-on": "^7.2.0"
  }
}
